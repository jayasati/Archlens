{
  "name": "@archlens/api",
  "version": "0.0.0",
  "private": true,
  "description": "Archlens NestJS backend API",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint . --no-error-on-unmatched-pattern",
    "test": "echo \"no tests yet\" && exit 0",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  }
}
